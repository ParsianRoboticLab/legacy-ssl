#include "gpsimulator.h"

CGPSimulator::CGPSimulator()
{
/*    QVector<int> cp;
    if (!context.create())
        qFatal("Could not create OpenCL context");
    QSize sz = QSize(im_width, im_height);
    program = context.buildProgramFromSourceFile(QLatin1String("cl/simulator.c"));
    dstImageBuffer = context.createImage2DDevice(QImage::Format_ARGB32, sz, QCLMemoryObject::WriteOnly);
    weights = context.createVector<float>(Features_Count);
    obsx = context.createVector<float>(max_obs_count);
    obsy = context.createVector<float>(max_obs_count);
    weight12 = context.createVector<float>(HiddenLayerSize*Features_Count);
    weight23 = context.createVector<float>(HiddenLayerSize);
    bias2 = context.createVector<float>(HiddenLayerSize);
    cc = context.createVector<int>(3);
    evalpoint = program.createKernel("evalpoint");
    evalpoint.setGlobalWorkSize(sz);
    evalpoint.setLocalWorkSize(hgaussian.bestLocalWorkSizeImage2D());*/
}


